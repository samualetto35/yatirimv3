[build]
  command = "npm run build"
  publish = "dist"

# Optional: better local dev defaults (Netlify CLI)
[dev]
  command = "npm run dev"
  port = 5173
  publish = "dist"

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


